/* import { Input } from "antd";
import React from "react";

function FormGenerator(item, actionMap) {
	const { type } = item;
	const isSmallScreen = window.innerWidth < 1440;

	const mapping = {
		INPUT: <Input />
	};
	return mapping[type];
}

export default FormGenerator;
 */
